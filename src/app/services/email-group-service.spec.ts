import { EmailGroupService } from './email-group-service';

describe('EmailGroupService', () => {
  it('should create an instance', () => {
    expect(new EmailGroupService()).toBeTruthy();
  });
});
